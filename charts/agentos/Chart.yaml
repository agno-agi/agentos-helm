apiVersion: v2
name: agentos
description: AgentOS — one agent backend for every frontend, built on Agno. REST + MCP + chat interfaces, Postgres (pgvector) persistence, in-process scheduler.
type: application
version: 0.1.0
appVersion: "1.0.0"
home: https://github.com/agno-agi/agentos-helm
sources:
  - https://github.com/agno-agi/agentos-helm
keywords:
  - agno
  - agentos
  - ai-agents
  - mcp
